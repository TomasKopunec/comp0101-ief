{
  "name": "@grnsft/if-optimisation-models",
  "version": "0.1.0",
  "description": "Optimisation models for IF (Impact Framework)",
  "main": "build/index.js",
  "scripts": {
    "start": "npm run install-and-exec:local",
    "build": "rm -rf build && tsc --project tsconfig.build.json",
    "build-with-copy": "rm -rf build && tsc --project tsconfig.build.json && cp -r ./data ./examples ../if/",
    "install2if": "./install2if.sh local",
    "install2if:local": "./install2if.sh local",
    "install2if:dev": "./install2if.sh dev",
    "if": "yarn impact-engine",
    "if:local": "yarn impact-engine",
    "if:dev": "cd ../if && yarn impact-engine",
    "intall-if": "npm run install2if && yarn impact-engine",
    "install-and-exec:local": "./install2if.sh local && yarn impact-engine",
    "install-and-exec:dev": "./install2if.sh dev && cd ../if && yarn impact-engine",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "impact-framework",
    "typescript"
  ],
  "author": "UCL IEF Team",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.3.3",
    "@grnsft/if": "^0.1.8",
    "@grnsft/if-models": "^0.1.3",
    "@grnsft/if-unofficial-models": "^0.1.3"
  },
  "dependencies": {
    "dotenv": "^16.3.2",
    "js-yaml": "^4.1.0",
    "zod": "^3.22.4",
    "typescript-cubic-spline": "^1.0.0"
  }
}
