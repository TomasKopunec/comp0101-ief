{
  "name": "@grnsft/if-optimisation-models",
  "version": "0.1.0",
  "description": "Optimisation models for IF (Impact Framework)",
  "main": "build/index.js",
  "scripts": {
    "start": "npm run if",
    "build": "rm -rf build && tsc --project tsconfig.build.json",
    "install2if": "./install2if.sh local",
    "install2if:local": "./install2if.sh local",
    "install2if:dev": "./install2if.sh dev",
    "install2if:global": "./install2if.sh global",
    "if": "yarn impact-engine",
    "if:local": "yarn impact-engine",
    "if:dev": "cd ../if && yarn impact-engine",
    "if:global": "impact-engine",
    "intall-if": "npm run install2if && yarn impact-engine",
    "install-and-exec:local": "npm run install2if:local && yarn impact-engine",
    "install-and-exec:dev": "npm run install2if:dev && cd ../if && yarn impact-engine",
    "install-and-exec:global": "npm run install2if:global && impact-engine",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "impact-framework",
    "typescript"
  ],
  "author": "UCL IEF Team",
  "license": "ISC",
  "devDependencies": {
    "@grnsft/if": "^0.1.8",
    "@grnsft/if-models": "^0.1.3",
    "@grnsft/if-unofficial-models": "^0.1.3",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "dotenv": "^16.3.2",
    "js-yaml": "^4.1.0",
    "qs": "^6.11.2",
    "typescript-cubic-spline": "^1.0.0",
    "zod": "^3.22.4"
  }
}
